{
  "name": "midi-realplayer",
  "displayName": "MIDI RealPlayer",
  "description": "A real multi-track MIDI player for VS Code with SoundFont playback, piano-roll and track views, mixer controls, and WAV export.",
  "version": "0.2.0",
  "publisher": "vaclis",
  "license": "MIT",
  "pricing": "Free",
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#071C2C",
    "theme": "dark"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vaclisinc/midi-realplayer-vscode.git"
  },
  "homepage": "https://github.com/vaclisinc/midi-realplayer-vscode#readme",
  "bugs": {
    "url": "https://github.com/vaclisinc/midi-realplayer-vscode/issues"
  },
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "midi",
    "midi player",
    "midi viewer",
    "midi playback",
    "midi visualizer",
    "multitrack midi",
    "piano roll",
    "soundfont",
    "music",
    "transcription"
  ],
  "activationEvents": [
    "onCustomEditor:midiRealPlayer.viewer"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "midiRealPlayer.viewer",
        "displayName": "MIDI RealPlayer",
        "selector": [
          {
            "filenamePattern": "*.mid"
          },
          {
            "filenamePattern": "*.midi"
          }
        ],
        "priority": "default"
      }
    ],
    "commands": [
      {
        "command": "midiRealPlayer.selectSoundFont",
        "title": "MIDI: Select SoundFont"
      }
    ],
    "configuration": {
      "title": "MIDI RealPlayer",
      "properties": {
        "midiRealPlayer.soundFontPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to an SF2, SF3, or DLS sound bank. Leave empty to use the bundled GeneralUser GS bank."
        }
      }
    }
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "package": "npm run build && vsce package --no-dependencies"
  },
  "dependencies": {
    "spessasynth_core": "^4.3.15",
    "spessasynth_lib": "^4.3.11"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/vscode": "^1.100.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
